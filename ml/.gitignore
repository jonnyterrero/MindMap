# Python env / cache
.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local data, generated artifacts, and any real exports (NEVER commit user data)
data/
artifacts/
*.parquet
*.duckdb
reports/*.json
reports/*.png

# Local secrets
.env
.env.local

# Exception: the graph calibrator is a reviewed model artifact fit on the
# committed synthetic gold set (no user data) — prod needs it from a clean checkout.
!mindmap_ml/graph/artifacts/
mindmap_ml/graph/artifacts/*
!mindmap_ml/graph/artifacts/graph_calibrator.json
